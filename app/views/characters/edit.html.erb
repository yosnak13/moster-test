<h1>学習時間を記入しよう</h1>
<p>現在の経験値：<%= @character.character_exp %></p>
<%= form_with(model: @character, local: true) do |f| %>
  <%= f.number_field :character_exp %>
  <%= f.hidden_field :character_exp, value: @character.character_exp %>
  <%= f.submit %>
<% end %>
